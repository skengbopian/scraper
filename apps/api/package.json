{
  "name": "@scraper/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NestJS API \u2014 the safety gates as framework guards",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@scraper/core": "workspace:*",
    "@scraper/db": "workspace:*",
    "@prisma/client": "^5.20.0",
    "@nestjs/common": "^10.4.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/platform-express": "^10.4.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.0",
    "@scraper/doc-sandbox": "workspace:*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0",
    "@types/express": "^4.17.21"
  }
}