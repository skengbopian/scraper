{
  "name": "@scraper/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable workflows behind the WorkflowEngine interface",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "//probe:store": "A real round trip against the configured object store — write, read back, verify the SHA-256, delete, confirm gone. readiness runs the equivalent inline for `fs`; for `s3` it needs credentials and a network, so it names this command instead of guessing.",
    "probe:store": "node dist/providers/object-store/probe.js"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "@scraper/api": "workspace:*",
    "@scraper/core": "workspace:*",
    "nodemailer": "^9.0.5",
    "pdf-lib": "^1.17.1",
    "pg-boss": "^10.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}