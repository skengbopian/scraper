{
  "name": "@scraper/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable workflows behind the WorkflowEngine interface",
  "scripts": { "build": "tsc -p tsconfig.json", "test": "vitest run --passWithNoTests", "lint": "tsc -p tsconfig.json --noEmit" },
  "dependencies": { "@scraper/core": "workspace:*" },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0", "@types/node": "^22.0.0" }
}
